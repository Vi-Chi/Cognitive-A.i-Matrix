[package]
name = "compute_ledger"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = "0.10.27"
ic-cdk = "0.20.1"
ic0 = "1.1.0"
serde = { version = "1", features = ["derive"] }
